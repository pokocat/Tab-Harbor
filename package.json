{
  "name": "tabharbor-extension",
  "version": "0.1.0",
  "private": true,
  "description": "TabHarbor is a Chrome extension for taming tab overload with session archiving and duplicate cleanup.",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && node scripts/prepare-dist.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "typescript": "^5.9.3"
  }
}
